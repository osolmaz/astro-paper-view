typescript:
  coverage:
    threshold: 85
  complexity:
    max: 8
  dry:
    max_findings: 0
    paths: [src]
    copied_blocks:
      enabled: true
      min_tokens: 100
  dependency_boundaries:
    - from: src/client
      allow: [src/client]
